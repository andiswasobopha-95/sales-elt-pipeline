name: 'sales_elt_pipeline'
version: '1.0.0'
config-version: 2

profile: 'sales_elt_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sales_elt_pipeline:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
